<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE urlrewrite
    PUBLIC "-//tuckey.org//DTD UrlRewrite 4.0//EN"
    "http://www.tuckey.org/res/dtds/urlrewrite4.0.dtd">
<!--
This file is intended for use with Tuckey UrlRewriteFilter (http://tuckey.org/urlrewrite/) for Tomcat Servlet Containers
Tuckey is used by commandbox (https://www.ortussolutions.com/products/commandbox) which can greatly speed your local development.
In order to use this file with commandbox, we recommend creating a server.json file in your webroot with this starter markup:

{
    "name":"myWebApp",
    "force":true,
    "web":{
        "http":{
            "host":"localhost",
            "port":60001
        },
        "rewrites":{
            "enable":true,
            "config":"urlrewrite.xml"
        }
    }
}

See more at http://docs.cfwheels.org/docs/commandbox and https://ortus.gitbooks.io/commandbox-documentation/content/embedded_server/serverjson.html
If you do not use Tuckey or Commandbox, this file can be safely deleted.
-->
<urlrewrite>
  <!-- Rule to rewrite stylesheet URLs -->
  <rule enabled="true">
    <name>Stylesheet rewrite rule</name>
    <from>^/css/(.*)$</from>
    <to type="forward">/stylesheets/$1</to>
  </rule>

  <!-- Rule to rewrite javascript URLs -->
  <rule enabled="true">
    <name>Javascript rewrite rule</name>
    <from>^/js/(.*)$</from>
    <to type="forward">/javascripts/$1</to>
  </rule>

  <!-- Rule to handle image requests securely -->
  <rule enabled="true">
    <name>Image rewrite rule</name>
    <from>^/img/(.*)$</from>
    <to type="forward">/images/$1</to>
  </rule>

  <!-- Rule to handle image requests securely -->
  <rule enabled="true">
    <name>Guides Image rewrite rule</name>
    <from>^/wheels/guides-assets/(.*)$</from>
    <to type="forward">/images/$1</to>
  </rule>

  <!-- Rule to serve sitemap.xml from sitemap folder -->
  <rule enabled="true">
    <name>Sitemap serve rule</name>
    <from>^/sitemap\.xml$</from>
    <to type="forward">/sitemap/sitemap.xml</to>
  </rule>

  <!-- New rule for converting format after dot to format query parameter -->
  <rule enabled="true">
    <name>Convert dot to format parameter</name>
    <condition type="request-uri" operator="notequal">^/miscellaneous/</condition>
    <from>^/(.*)\\.(\\w+)$</from>
    <to>/$1?format=$2</to>
  </rule>

  <!-- Main framework router rule - should be last -->
  <rule enabled="true">
    <name>Wheels pretty URLs</name>
    <condition type="request-uri" operator="notequal">^/(cf_script|flex2gateway|jrunscripts|CFIDE/administrator|lucee/admin|cfformgateway|cffileservlet|lucee|miscellaneous|files|img|images|css|js|wheels/public/assets|wheels/guides-assets|robots.txt|favicon.ico|sitemap.xml|index.cfm)</condition>
    <from>^/(.*)$</from>
    <to type="passthrough">/index.cfm/$1</to>
  </rule>
</urlrewrite>
