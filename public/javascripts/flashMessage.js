const tempDiv=document.createElement('div');tempDiv.innerHTML=html;const pTag=tempDiv.querySelector('.flash-messages p');if(pTag){const classList=pTag.className;const flashType=classList.split('-')[0];const flashMessage=pTag.textContent.trim();if(flashType=="success"||flashType=="warning"){notifier.show(flashType,flashMessage,flashType,"",4000)}else if(flashType=="error"){notifier.show("Error",flashMessage,"danger","",4000)}else{notifier.show("Info",flashMessage,info,"",4000)}}